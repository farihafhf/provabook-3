export enum OrderStatus {
  UPCOMING = 'upcoming',
  RUNNING = 'running',
  COMPLETED = 'completed',
  ARCHIVED = 'archived',
  CANCELLED = 'cancelled',
}

export enum OrderCategory {
  UPCOMING = 'upcoming',
  RUNNING = 'running',
  ARCHIVED = 'archived',
}
