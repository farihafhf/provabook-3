export enum PIStatus {
  DRAFT = 'draft',
  SENT = 'sent',
  REVISED = 'revised',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
}

export enum LCStatus {
  PENDING = 'pending',
  RECEIVED = 'received',
  AMENDED = 'amended',
  EXPIRED = 'expired',
  UTILIZED = 'utilized',
}
